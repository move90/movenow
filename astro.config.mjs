// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // Used for the canonical URL and Open Graph tags — change if the site moves.
  site: 'https://plan.move90.com',
  output: 'static',
});
